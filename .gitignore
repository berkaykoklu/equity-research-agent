.venv/
__pycache__/
*.pyc
.env
.env.*
.pytest_cache/
.mypy_cache/
.ruff_cache/
.cache/
.superpowers/
# Internal build plans and specs. Kept locally, not published: they are
# scaffolding for how the repo was built, not documentation of what it does.
docs/superpowers/
.DS_Store
dist/
*.egg-info/
.vercel
.env*

# Last, so it survives the broader .env* rule above (which `vercel link` adds).
# .env.example carries no secrets and is what the quickstart tells you to copy.
!.env.example
