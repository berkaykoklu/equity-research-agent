# SEC requires a real contact address in the User-Agent or it will block you
# SEC rejects requests whose User-Agent carries no real contact address.
# Put YOUR name and email here -- this file is public, so it holds neither.
EDGAR_USER_AGENT="Your Name your.email@example.com"
OPENAI_API_KEY=
VOYAGE_API_KEY=
DATABASE_URL=
OPIK_API_KEY=
OPIK_WORKSPACE=
